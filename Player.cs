class Player : Character
{
    public Player(int maxhp, int currenthp, int attack, int heal, string name)
        : base(maxhp, currenthp, attack, heal, name)
    {
        int areaattack = 5;
    }
}